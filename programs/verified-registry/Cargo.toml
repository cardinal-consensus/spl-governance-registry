[package]
name = "verified_registry"
version = "0.1.0"
description = "Solana verified registry"
edition = "2018"

[lib]
crate-type = ["cdylib", "lib"]
name = "verified_registry"

[features]
no-entrypoint = []
no-idl = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.18.0"
